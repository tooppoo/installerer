---
"@installerer/web": minor
"@installerer/core": minor
---

Generated installer `--help` now documents every option and a standard `curl | sh` install command (with `sh -s --` and review-first alternatives), and the Web UI shows a copyable "Standard curl install" command alongside the review-first alternative.
