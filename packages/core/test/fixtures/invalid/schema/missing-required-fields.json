{
  "description": "Required fields owner and checksum.fileName are missing.",
  "config": {
    "repo": "rellog",
    "binary": {
      "name": "rellog",
      "pathInArchive": "bin/rellog"
    },
    "archive": {
      "format": "tar.gz",
      "nameTemplate": "{repo}_{version}_{target}.tar.gz"
    },
    "checksum": {
      "algorithm": "sha256"
    },
    "targets": [{ "os": "linux", "arch": "x86_64" }]
  },
  "expectedErrors": [
    { "path": "$.owner", "reasonIncludes": "Required field is missing." },
    { "path": "$.checksum.fileName", "reasonIncludes": "Required field is missing." }
  ]
}
