/**
 * Façade kept for import compatibility; the implementation lives in
 * src/generatedInstaller/ split by generated-shell responsibility (issue #71).
 */
export { generateInstaller, previewArchiveNames } from "./generatedInstaller";
export { shellLiteral } from "./generatedInstaller/shell";
