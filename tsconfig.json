{
  "$schema": "https://raw.githubusercontent.com/SchemaStore/schemastore/refs/heads/master/src/schemas/json/tsconfig.json",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Repository-level tooling scripts only (run under Bun). Workspace
    // packages have their own package-scoped tsconfig.
    "lib": ["ESNext"],
    "types": ["bun"]
  },
  "include": ["scripts/**/*.ts"]
}
