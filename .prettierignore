# Committed snapshot fixtures are byte-for-byte comparison targets written by
# test code (see packages/core/test/helpers/snapshot.ts). They must not be
# reformatted.
packages/core/test/snapshots/*.json
